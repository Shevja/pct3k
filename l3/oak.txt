
Кластер Oak.
Количество вычислительных узлов: 6
Общий объем оперативной памяти (GiB): 24
Объем оперативной памяти на одном узле (GiB): 4

Предельные размеры квадратной матрицы A и вектора B,
которые можно перемножать на одном узле кластера:   
        
        4 * (n^2 + 2n) < 24 * 2^30 * 0.8;
        n = 71 790;

На 6 узлах кластера:

        4 * (n^2 / 6 + 2n) < 24 * 2^30 * 0.8;
        n = 175 845;